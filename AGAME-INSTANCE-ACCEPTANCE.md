# AGame 流程与实例集验收

- 首次基准：agame-scene-assets-baseline v3，36 个定义节点、8 个主阶段，保留稳定 ID 与原版本。新增目标/参考图等输入、独立基准入库、性能检查及全局约束。
- 衍生岛屿：agame-derived-islands v1，13 个定义节点、三级嵌套；选择已确认基准及目标，明确填写数量，顺序展开独立实例；交付止于初始 HIP。已校验 1/3/4 份展开、顺序前置与最终汇合，未启动生产。
- 两个已确认基准均在资源选择器可见；原 agame-scene-assets 全目录哈希前后相同。新版 MD 独立保存，旧版本文档及旧页面未保存编辑保留。
- 实例集侧栏按流程列出每次运行，展示冻结输入、图片、MD、约束、补充资料和历史成果。用户网页写入、AI MCP 写入分别标记来源。
- MCP 新增 workflow_instances / workflow_data_write，workflow_context 带 instanceData。完成节点自动保存 evidence 与成果附件；文件按哈希独立归档，网页上传单次 10MB，本地路径单文件 256MB。缺失、超限及外部链接均明确标记。
- 真正的 MCP 上报与网页用户记录在既有归档自检实例 f958f1bc-21b8-4103-abe9-3459eb21c06d 验证；原文件改写后归档内容仍可下载，服务重启后两条资料及附件完整，状态仍是已取消。测试不启动新实例或岛屿任务。
- 25 项自动测试通过，包含来源、CAS、幂等、附件隔离与校验、重试历史；TypeScript 与生产构建通过；codex-workflow Skill 校验通过。
- JSON 导出包含记录和附件索引，非完整项目压缩包。旧记录不自动补拷历史产物；聊天与工具输出需通过流程工具主动上报。
