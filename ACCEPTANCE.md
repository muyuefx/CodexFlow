# 验收记录

验证日期：2026-09-17。当前服务：http://127.0.0.1:43127 。

## 自动化

- TypeScript 检查、Vite 前端及 Node 服务构建通过。
- `npm test`：15 项全部通过。覆盖并行汇合、人工门禁、三层嵌套及重复子流程实例、依赖环、验收依据、竞争写入、幂等、MD 冲突与快照、暂停改版、影响范围、重试、服务重启及 MCP 协议。
- `scripts/transport-check.ts`：真实 HTTP MCP、SSE、来源检查和暂停应用版本通过；本机观测 SSE 通知延迟 **22.01ms**（单次验收测量，非持续性能保证）。测试流程已归档。
- 官方 Skill 校验脚本通过，已安装 `codex-workflow`。

## 浏览器实际操作

- 默认流程的第二层、第三层可双击进入，面包屑和深度正确。
- 普通节点双击打开 CodeMirror 编辑器，保存前显示原路径及差异，保存后保留历史。
- 草稿未保存时从外部修改文件，切换到其他节点再返回：旧草稿保留，仍使用旧基准 hash，保存被冲突检查拦截；磁盘内容未被覆盖。
- 联动演示中父流程与执行中的子节点同步显示状态；人工验收节点显示等待人工。
- 通过浏览器完成模拟人工验收，随后经 AI MCP 接口提交最终总结；刷新页面和重启服务后记录保留。

## 真实 Codex 接入

全局 MCP 名称：`codex-flow`；传输：Streamable HTTP；地址：`http://127.0.0.1:43127/mcp`。

Codex CLI 使用 `gpt-5.6-luna`、low 推理档位，在只读、临时会话中实际调用 `workflow_list` 与 `workflow_context`，确认：

- 演示运行 `526dcdb9-ef58-4581-ad3f-7bccf3209ddf` 为 completed。
- 六个节点全部 completed。
- review 节点 completion=user_only，completedBy=user。

首次测试缺少 MCP 只读属性声明，调用被审批策略拦截；补全准确的工具注解后复测通过。未通过放宽审批或关闭保护来解决。

完整结果保存在 `logs/codex-smoke.md`、`logs/integration-state.json`、`logs/transport-check.json`。演示流程名为“联动验收 · 三级子流程”，可在界面检查时间线。

人工节点由自动化操作浏览器验证用户操作通道，时间线明确标注为验收测试；这不表示用户已经验收任何真实项目成果。结构化流程由本次 Codex 会话设计，经实际 MCP 客户端保存和推进，未声称整个演示由 CLI 自主完成。

## 使用边界

- 现有 Codex 会话需重载 MCP，或开新对话，才会获得新增工具与 Skill；服务已经配置并运行。
- 页面中的“AI 生成”和“继续”提供当前 Codex 对话的指令，不另起付费 API 调用，也不会自动唤醒停止的对话。
- 并行是依赖关系；实际执行仍由 Codex 结合资源约束安排。
- 暂停/取消不强行终止外部 DCC 或命令。未结束的运行在服务重启后必须恢复。
- JSON 导出是可读镜像；外部编辑 JSON 后需通过导入载入。Markdown 编辑直接写回原文件，并保存历史。
