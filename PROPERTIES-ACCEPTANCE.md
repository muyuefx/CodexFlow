# 通用节点属性验收

更新日期：2026-09-18。更改的是通用 schema、状态引擎、存储/API/MCP 与前端表单；未给 AGame 流程写专用运行逻辑，未修改原 agame-scene-assets Skill。

## 已验证

- 任意节点可定义 `properties` 与 `inputValues`，支持文字、长文字、数字、开关、选项、图片、资源；默认值与用户填写值分开。必填、类型、上下限、整数及重复标识由共享校验处理。
- 在实际浏览器中新增“补充要求”文字属性及默认值，填写目标、上传 PNG、选择“松林工业岛”资源，把生成数量从默认 3 改成 4。保存后 API 回读一致，刷新后继续使用。
- 用户上传图片与 AI 通过 MCP 导入图片均存到独立内容寻址文件。已有两份基准图及档案只读复制到独立资源库；源 Skill 全目录生成前后哈希和文件集合一致。
- MCP 真实协议保存由 AI 定义的属性、读取实际值、返回参考图片；现在可发现 13 个工具。
- 批量子流程由整数属性决定展开数，有并行/顺序两种模式；独立实例的依赖、完成门禁、父子高亮和实例选择器有效。
- 真实验收运行 `4d3d6b37-e68b-406b-ba7f-02a0ccff504b`：4 份独立输入摘要经 MCP 写入并回读；第 1 份完成时第 2 份运行、第 3 份待执行；最终 6/6 节点完成。没有执行实际场景生产或付费任务。记录在 `logs/property-acceptance.json`。
- 输入、资源元数据、图片及 MD 随运行冻结；修改定义不改变旧运行。暂停应用新版本前展示影响，拒绝过期预览；输入改变重置受影响节点和下游，旧结果保留。
- 右键“修改该节点流程 · Codex”生成节点、输入定义与值、连线和子流程交接，带版本与未保存标记；复制后实际粘贴核对文本完整。
- 修复受控 React Flow 节点尺寸丢失导致 SSE 状态刷新后节点隐藏的问题，执行视图和实例切换均经截图检查。

## 验证命令

`npm.cmd run build` 通过；`npm.cmd test` 20 项通过。新增测试覆盖默认值与 false/数字覆盖、必填/类型拒绝、3→4 独立展开与汇合、顺序依赖、图片来源变更隔离、资源与 MD 快照、暂停影响与陈旧预览拒绝、重启恢复及节点交接。

## 使用边界

- 当前独立网页没有直接填入 Codex 输入栏的已接入接口；右键交接需要用户粘贴，不伪称自动发送。
- 批量展开是启动时的有界实例生成（1–100），不是自动循环；共享工具应选择顺序执行。
- JSON 导出保留引用，不是自包含图片/资源包。跨库使用需迁移对应资源与文档或重新选择。
- 通用示例“节点输入 · 可变数量示例”提供可编辑接口；既有工作流不被批量改写。未来 AI 生成使用更新后的 codex-workflow Skill 与 MCP schema。
